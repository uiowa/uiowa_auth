uiowa_auth.settings:
  type: config_object
  label: 'UIowa authentication settings'
  mapping:
    role_mappings:
      label: 'Role mappings'
      type: sequence
      sequence:
        - type: string
    allow_all:
      label: 'Whether or not to allow all valid HawkID users to authenticate'
      type: boolean
